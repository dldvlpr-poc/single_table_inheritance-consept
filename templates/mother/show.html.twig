{% extends 'base.html.twig' %}

{% block title %}Mother{% endblock %}

{% block body %}
    <h1>Mother</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mother.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ mother.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_mother_index') }}">back to list</a>

    <a href="{{ path('app_mother_edit', {'id': mother.id}) }}">edit</a>

    {{ include('mother/_delete_form.html.twig') }}
{% endblock %}
