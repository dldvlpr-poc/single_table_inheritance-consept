<form method="post" action="{{ path('app_mother_delete', {'id': mother.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mother.id) }}">
    <button class="btn">Delete</button>
</form>
